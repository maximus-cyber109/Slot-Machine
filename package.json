{
  "name": "pb-days-arcade-complete",
  "version": "5.0.0",
  "description": "Complete auto-detecting slot machine with prize allocation system",
  "main": "main.html",
  "scripts": {
    "build": "echo 'Static site ready'",
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@octokit/rest": "^20.0.2"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
